/*
These javascript api you will use throughout you javascript journey especially. 

Array:
push(), pop(), shift(), unshift(), splice(), slice(), concat(), forEach(), map(), filter(), reduce(), find(), sort(), etc.

String:
length, indexOf(), lastIndexOf(), slice(), substring(), replace(), split(), trim(), toUpperCase(), toLowerCase(), etc.

Object:
Object.keys(), Object.values(), Object.entries(), hasOwnProperty(), assign(), freeze(), seal(), defineProperty(), etc.

Math:
round(), ceil(), floor(), random(), max(), min(), pow(), sqrt(), etc.

Date:
getDate(), getMonth(), getFullYear(), getHours(), getMinutes(), getSeconds(), getTime(), setDate(), setMonth(), setFullYear(), setHours(), setMinutes(), setSeconds(), etc.

JSON:
parse(), stringify()

Set:
add(), delete(), has(), size(), forEach(), etc.

Map:
set(), get(), delete(), has(), size(), forEach(), etc.

Promise:
then(), catch(), finally(), all(), race(), etc.

Fetch:
fetch(), then(), catch(), etc.

RegExp:
test(), exec(), match(), replace(), search(), split(), etc.

Number:
parseInt(), parseFloat(), toFixed(), toPrecision(), etc.

SetTimeout and SetInterval:
setTimeout(), clearTimeout(), setInterval(), clearInterval(), etc.

Console:
log(), error(), warn(), info(), table(), etc.
*/
